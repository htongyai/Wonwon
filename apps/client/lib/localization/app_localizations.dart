// Re-export from shared package so the entire app uses the same AppLocalizations type.
// This ensures .tr(context) works in both shared and client code.
export 'package:shared/localization/app_localizations.dart';
